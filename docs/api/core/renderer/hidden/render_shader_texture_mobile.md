# Renderer::render_shader_texture_mobile()

Uniffi variant of `render` targeting a `TextureTarget`. See
`render_shader_mobile` for the rationale behind the concrete method split.

## Example

```rust
// hidden file; no public example
```
