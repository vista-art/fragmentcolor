# Renderer.create_external_texture_from_html_video(video)

This API is only available on the Web JavaScript binding.

## Example

```python
# This API is only available on the Web JavaScript binding.
# On Python, upload decoded video frames with Texture.write() or Texture.write_with().
```
