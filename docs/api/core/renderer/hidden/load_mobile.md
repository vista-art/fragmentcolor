# Renderer.load_mobile

Platform-specific method for Swift and Kotlin (via uniffi).
Hidden from public website, available for IDE hover via lsp_doc.

## Example

```rust
// hidden file; no public example
```
