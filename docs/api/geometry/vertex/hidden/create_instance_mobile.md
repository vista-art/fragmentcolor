# Vertex::create_instance_mobile()

Uniffi-friendly `createInstance` method for Swift (iOS) and Kotlin (Android).

Hidden from the public website; Swift/Kotlin users call `vertex.createInstance()`.

## Example

```rust
// hidden file; no public example
```
