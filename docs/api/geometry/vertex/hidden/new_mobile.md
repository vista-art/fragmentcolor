# Vertex::new_mobile(position)

Uniffi-friendly constructor for Swift (iOS) and Kotlin (Android).

Accepts a `Vec<f32>` of length 2, 3, or 4. Hidden from the public website;
Swift/Kotlin users call `Vertex.new(position)` via the extension helpers that
forward to this entry point.

## Example

```rust
// hidden file; no public example
```
