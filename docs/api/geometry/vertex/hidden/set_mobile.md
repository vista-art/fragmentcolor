# Vertex::set_mobile(key, value)

Uniffi-friendly `set` method for Swift (iOS) and Kotlin (Android).

Accepts a `VertexValue` enum directly. Hidden from the public website;
Swift/Kotlin users call `vertex.set(key, value)`.

## Example

```rust
// hidden file; no public example
```
