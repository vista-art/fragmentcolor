# Auto-generated by build.rs (scripts/parity.rs).
# Lists currently-acknowledged parity gaps. The audit fails on any
# gap or unresolved binding that is NOT a subset of the entries here.
# Trim entries as Phase 3 closes them; regenerate with:
#   FC_PARITY_REWRITE_BASELINE=1 cargo build --lib
#
# Line format:
#   gap:<canonical_doc>:<comma_separated_missing_platforms>
#   unresolved:<lsp_doc_path>:<comma_separated_credited_platforms>

gap:docs/api/core/shader/default.md:web
gap:docs/api/targets/texture_target/get_image.md:web
gap:docs/api/targets/texture_target/resize.md:web
gap:docs/api/targets/texture_target/size.md:web
