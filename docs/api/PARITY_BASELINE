# Auto-generated by build.rs (scripts/parity.rs).
# Lists currently-acknowledged parity gaps. The audit fails on any
# gap or unresolved binding that is NOT a subset of the entries here.
# Trim entries as Phase 3 closes them; regenerate with:
#   FC_PARITY_REWRITE_BASELINE=1 cargo build --lib
#
# Line format:
#   gap:<canonical_doc>:<comma_separated_missing_platforms>
#   unresolved:<lsp_doc_path>:<comma_separated_credited_platforms>
#   unlabeled:<workspace-relative-file>:<fn_name>

gap:docs/api/targets/texture_target/texture.md:python,swift,kotlin
# GltfSource builder filters — Rust-only for now. Bindings reach the same
# behaviour through their `Scene.load(...)` shim; exposing the typed
# builder cross-platform is a separate follow-up (likely an options-bag
# parameter on `Scene.load`, not a 1:1 mirror of the Rust builder).
gap:docs/api/scene/gltf_source/cameras.md:web,python,swift,kotlin
gap:docs/api/scene/gltf_source/lights.md:web,python,swift,kotlin
