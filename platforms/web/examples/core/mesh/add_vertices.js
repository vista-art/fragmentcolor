import { {Mesh, Vertex, Position} } from "fragmentcolor";
const m = new Mesh();
m.addVertices([;
  Vertex.fromPosition(Position.Pos2([0.0, 0.0])),;
  Vertex.fromPosition(Position.Pos2([1.0, 0.0]));
]);