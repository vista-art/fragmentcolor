[licenses]
confidence-threshold = 0.93
allow = [
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "MIT",
    "MIT-0",
    "CC0-1.0",
    "MPL-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Zlib",
    "Unicode-3.0",
    # webpki-roots ships the Mozilla CA bundle under the Linux Foundation's
    # Community Data License Agreement — Permissive, v2.0. It's a pure
    # data set, not code, and the CDLA-Permissive-2.0 terms (no copyleft,
    # no attribution requirement on derivative data) are at least as
    # permissive as MIT for the embedded data we redistribute. Pulled in
    # transitively via ureq's `rustls` feature on desktop targets.
    "CDLA-Permissive-2.0",
]
