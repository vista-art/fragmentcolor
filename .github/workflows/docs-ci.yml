name: Docs CI

on:
  pull_request:
    paths:
      - 'docs/website/**'
      - '.github/workflows/docs-ci.yml'

jobs:
  deps-up-to-date:
    name: Dependencies up-to-date
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
          cache-dependency-path: docs/website/pnpm-lock.yaml

      - name: Install dependencies (docs/website)
        run: pnpm --dir docs/website install

      - name: Fail if outdated dependencies (docs/website)
        run: pnpm --dir docs/website outdated

  build-docs:
    name: Build docs site
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
          cache-dependency-path: docs/website/pnpm-lock.yaml

      - name: Install dependencies (docs/website)
        run: pnpm --dir docs/website install

      - name: Build docs (astro)
        run: pnpm --dir docs/website build

